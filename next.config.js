const dotenv = require("dotenv");
dotenv.config;

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your other config options
  // Make sure there's no port setting here that would override your server.js
};

module.exports = nextConfig;
